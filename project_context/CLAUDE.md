# CLAUDE.md — <НАЗВАНИЕ ПРОЕКТА>

> Этот файл Claude Code **читает автоматически при каждом новом окне** — держи его
> сжатым (навигатор, не свод знаний). Здесь только: что за проект, где что, и какой
> план сейчас активен. Детали живут в других файлах — не дублировать, не раздувать.
>
> **Cursor:** аналогичный навигатор — `.cursor/rules/agent-core.mdc` (создаётся из `cursor-rule.mdc`).
> Держи оба синхронными — они описывают одно и то же.

## Что за проект (3–5 строк)

<Для кого, какую проблему решает, что на выходе. Коротко.>

## Стек и где что

| Часть | Стек | Папка |
|---|---|---|
| Frontend | <> | <> |
| Backend | <> | <> |
| Контекст/планы | — | <папка плана, напр. `project-context/`> |

Как поднять локально: <команда или ссылка>.

## 🎯 Активный план (что продолжать)

**Фича:** <название>
**Статус — единственный источник правды:** [`<путь>/04_STATE.md`](<путь>/04_STATE.md)
→ Открой его, возьми первый шаг `TODO`. Точка входа фичи: `<путь>/00_START_HERE.md`.

<Когда фича закрыта — замени на следующую активную или напиши «нет активного плана».>

## 🔒 Правила (неизменны)

- План + критическая оценка → согласование → код. Без одобрения не реализовывать.
- Не ломать существующее — только дополнять.
- Один факт — один файл. Прогресс — только в `04_STATE.md`.
- Скиллы под задачу — точечно из банка (`_STARTER-KIT/skills-bundle/00_SKILLS_MAP.md`).
